// GENERATED FILE: DO NOT EDIT

provider "aws" {
  alias = "0"
  region = "${local.lambda_0_region}"
}

provider "aws" {
  alias = "1"
  region = "${local.lambda_1_region}"
}

provider "aws" {
  alias = "2"
  region = "${local.lambda_2_region}"
}

provider "aws" {
  alias = "3"
  region = "${local.lambda_3_region}"
}

provider "aws" {
  alias = "4"
  region = "${local.lambda_4_region}"
}

provider "aws" {
  alias = "5"
  region = "${local.lambda_5_region}"
}

provider "aws" {
  alias = "6"
  region = "${local.lambda_6_region}"
}

provider "aws" {
  alias = "7"
  region = "${local.lambda_7_region}"
}

provider "aws" {
  alias = "8"
  region = "${local.lambda_8_region}"
}

provider "aws" {
  alias = "9"
  region = "${local.lambda_9_region}"
}
