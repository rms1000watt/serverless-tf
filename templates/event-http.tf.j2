// GENERATED FILE: DO NOT EDIT

resource "aws_api_gateway_rest_api" "global" {
  name  = "${local.service_name}"

  count = "${ {%- for i in range(max_functions) %}local.api_gateway_{{i}} || {% endfor %}false ? 1 : 0}"
}

resource "aws_api_gateway_account" "global" {
  cloudwatch_role_arn = "${aws_iam_role.api_gateway_global.arn}"

  count = "${ {%- for i in range(max_functions) %}local.api_gateway_{{i}} || {% endfor %}false ? 1 : 0}"
}
{%- for i in range(max_functions) %}

resource "aws_api_gateway_resource" "{{i}}" {
  rest_api_id = "${aws_api_gateway_rest_api.global.id}"
  parent_id   = "${aws_api_gateway_rest_api.global.root_resource_id}"
  path_part   = "${local.api_gateway_{{i}}_path}"

  count = "${local.api_gateway_{{i}}_count}"
}

resource "aws_api_gateway_method" "{{i}}_n_auth" {
  rest_api_id   = "${aws_api_gateway_rest_api.global.id}"
  resource_id   = "${aws_api_gateway_resource.{{i}}.id}"
  http_method   = "${local.api_gateway_{{i}}_method}"
  authorization = "${local.api_gateway_{{i}}_authorization}"

  count = "${local.api_gateway_{{i}}_count + local.api_gateway_{{i}}_authorizer_count == 1 ? 1 : 0}"
}

resource "aws_api_gateway_method" "{{i}}_w_auth" {
  rest_api_id   = "${aws_api_gateway_rest_api.global.id}"
  resource_id   = "${aws_api_gateway_resource.{{i}}.id}"
  http_method   = "${local.api_gateway_{{i}}_method}"
  authorization = "${local.api_gateway_{{i}}_authorization}"
  authorizer_id = "${aws_api_gateway_authorizer.{{i}}.id}"

  count = "${local.api_gateway_{{i}}_count + local.api_gateway_{{i}}_authorizer_count == 2 ? 1 : 0}"
}

resource "aws_api_gateway_integration" "{{i}}_n_auth" {
  rest_api_id             = "${aws_api_gateway_rest_api.global.id}"
  resource_id             = "${aws_api_gateway_method.{{i}}_n_auth.resource_id}"
  http_method             = "${aws_api_gateway_method.{{i}}_n_auth.http_method}"
  type                    = "AWS_PROXY"
  integration_http_method = "POST"
  uri                     = "arn:aws:apigateway:${data.aws_region.current.name}:lambda:path/2015-03-31/functions/${local.lambda_{{i}}_arn}/invocations"

  count = "${local.api_gateway_{{i}}_count + local.api_gateway_{{i}}_authorizer_count == 1 ? 1 : 0}"
}

resource "aws_api_gateway_integration" "{{i}}_w_auth" {
  rest_api_id             = "${aws_api_gateway_rest_api.global.id}"
  resource_id             = "${aws_api_gateway_method.{{i}}_w_auth.resource_id}"
  http_method             = "${aws_api_gateway_method.{{i}}_w_auth.http_method}"
  type                    = "AWS_PROXY"
  integration_http_method = "POST"
  uri                     = "arn:aws:apigateway:${data.aws_region.current.name}:lambda:path/2015-03-31/functions/${local.lambda_{{i}}_arn}/invocations"

  count = "${local.api_gateway_{{i}}_count + local.api_gateway_{{i}}_authorizer_count == 2 ? 1 : 0}"
}

resource "aws_api_gateway_deployment" "{{i}}" {
  depends_on = [
    "aws_api_gateway_integration.{{i}}_n_auth",
    "aws_api_gateway_integration.{{i}}_w_auth",
  ]

  rest_api_id = "${aws_api_gateway_rest_api.global.id}"
  stage_name  = "${local.api_gateway_{{i}}_stage}"

  variables {
    deployed_at = "${timestamp()}"
  }

  count = "${local.api_gateway_{{i}}_count}"
}

resource "aws_api_gateway_method_settings" "{{i}}" {
  rest_api_id = "${aws_api_gateway_rest_api.global.id}"
  stage_name  = "${local.api_gateway_{{i}}_stage}"
  method_path = "*/*"

  settings {
    metrics_enabled = "${local.api_gateway_{{i}}_metrics}"
    logging_level   = "${local.api_gateway_{{i}}_logging_level}"
  }

  depends_on = [
    "aws_api_gateway_deployment.{{i}}",
    "aws_api_gateway_account.global",
  ]

  count = "${local.api_gateway_{{i}}_count}"
}

resource "aws_api_gateway_authorizer" "{{i}}" {
  name                   = "${local.api_gateway_{{i}}_authorizer}"
  rest_api_id            = "${aws_api_gateway_rest_api.global.id}"
  authorizer_uri         = "${local.api_gateway_{{i}}_authorizer_uri}"
  authorizer_credentials = "${aws_iam_role.api_gateway_{{i}}_authorizer.arn}"
  type                   = "REQUEST"

  count = "${local.api_gateway_{{i}}_authorizer_count}"
}

{%- endfor %}
